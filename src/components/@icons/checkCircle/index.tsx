import type { IconBaseProps } from 'react-icons'
import { AiOutlineCheckCircle } from 'react-icons/ai'

export const CheckCircleIcon = ({ ...rest }: IconBaseProps) => {
  return <AiOutlineCheckCircle {...rest} />
}
