import type { IconBaseProps } from 'react-icons'
import { AiOutlineCloseCircle } from 'react-icons/ai'

export const CloseCircleIcon = ({ ...rest }: IconBaseProps) => {
  return <AiOutlineCloseCircle {...rest} />
}
