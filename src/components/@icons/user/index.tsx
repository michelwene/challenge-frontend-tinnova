import type { IconBaseProps } from 'react-icons'
import { AiOutlineUserAdd } from 'react-icons/ai'

export const UserAddIcon = ({ ...rest }: IconBaseProps) => {
  return <AiOutlineUserAdd {...rest} />
}
