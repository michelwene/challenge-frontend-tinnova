import type { IconBaseProps } from 'react-icons'
import { AiOutlineLoading } from 'react-icons/ai'

export const LoadingIcon = ({ ...rest }: IconBaseProps) => {
  return <AiOutlineLoading {...rest} />
}
